# ==========================================
# 🧛 TMUX - UNIVERZÁLNÍ DRACULA CONFIG
# ==========================================

# ===== TPM (Plugin Manager) =====
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# ===== DRACULA THEME (oficiální plugin) =====
set -g @plugin 'dracula/tmux'

# Dracula customizace
set -g @dracula-show-powerline true
set -g @dracula-show-flags true
set -g @dracula-show-left-icon session
set -g @dracula-left-icon-padding 1
set -g @dracula-border-contrast true
set -g @dracula-cpu-usage true
set -g @dracula-ram-usage true
set -g @dracula-gpu-usage false
set -g @dracula-day-month true
set -g @dracula-military-time true
set -g @dracula-show-timezone false

# Modules v status baru (zleva doprava)
set -g @dracula-plugins "cpu-usage ram-usage time"

# ===== ZÁKLADNÍ NASTAVENÍ =====
set -g mouse on
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -g set-clipboard on

# Prefix
unbind C-b
set-option -g prefix C-a
bind C-a send-prefix

# History
set-option -g history-limit 10000
set -sg escape-time 0

# Start at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Status bar position
set -g status-position bottom
set -g status-interval 2

# ===== KEYBINDINGS =====
# Pane navigation
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Sync panes
bind e setw synchronize-panes

# Reload config
bind r source-file ~/.tmux.conf \; display "✓ Config reloaded!"

# ===== COPY MODE (VI) =====
setw -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# ===== TPM INIT (MUSÍ BÝT NA KONCI!) =====
run '~/.tmux/plugins/tpm/tpm'

# ==========================================
# 🧛 HOTOVO!
# ==========================================
#
# INSTALACE:
#   1. Ulož tento soubor jako ~/.tmux.conf
#   2. Nainstaluj TPM (pokud nemáš):
#      git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
#   3. Spusť tmux a zmáčkni: Ctrl+A, I (install plugins)
#   4. Restart tmuxu: tmux kill-server && tmux
#
# ==========================================
